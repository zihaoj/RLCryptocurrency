from setuptools import setup

setup(
    name='rl_cryptocurrency',
    version='0.0.1',
    install_requires=["gym", 'gym_rlcrptocurrency', "tensorflow", "numpy", "pandas", "tqdm", "matplotlib", "dill"]  # And any other dependencies foo needs
)
